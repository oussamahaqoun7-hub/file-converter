<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>المحول الخارق | All-in-One Tool</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/3.11.174/pdf.min.js"></script>
    <script src="https://unpkg.com/tesseract.js@v2.1.0/dist/tesseract.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">

    <style>
        body { font-family: 'Cairo', sans-serif; background: #f8fafc; }
        .service-card { transition: all 0.3s ease; cursor: pointer; border: 1px solid #e2e8f0; }
        .service-card:hover { transform: translateY(-5px); border-color: #6366f1; box-shadow: 0 10px 20px -5px rgba(99, 102, 241, 0.2); }
        .hidden-section { display: none; }
        .active-tab { border-bottom: 3px solid #6366f1; color: #6366f1; }
        .loader-dots div { animation-timing-function: cubic-bezier(0, 1, 1, 0); }
        @keyframes loader-dots { 0% { transform: scale(0); } 100% { transform: scale(1); } }
    </style>
</head>
<body class="bg-slate-50 text-slate-900">

    <div class="max-w-6xl mx-auto px-4 py-6">
        <header class="flex justify-between items-center mb-10 bg-white p-6 rounded-3xl shadow-sm">
            <h1 class="text-2xl font-black text-indigo-600">المحول الخارق <span class="text-slate-400 font-light">| SuperTools</span></h1>
            <div id="clock" class="text-sm font-bold text-slate-500"></div>
        </header>

        <div id="mainGrid" class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-10">
            <div onclick="showSection('img-conv')" class="service-card bg-white p-6 rounded-3xl text-center">
                <span class="text-3xl">🖼️</span>
                <h3 class="mt-2 font-bold">تحويل الصور</h3>
            </div>
            <div onclick="showSection('ocr-sec')" class="service-card bg-white p-6 rounded-3xl text-center">
                <span class="text-3xl">🔍</span>
                <h3 class="mt-2 font-bold">استخراج نص</h3>
            </div>
            <div onclick="showSection('qr-sec')" class="service-card bg-white p-6 rounded-3xl text-center">
                <span class="text-3xl">🏁</span>
                <h3 class="mt-2 font-bold">مولد QR</h3>
            </div>
            <div onclick="showSection('pdf-sec')" class="service-card bg-white p-6 rounded-3xl text-center">
                <span class="text-3xl">📄</span>
                <h3 class="mt-2 font-bold">PDF أدوات</h3>
            </div>
        </div>

        <section id="ocr-sec" class="hidden-section bg-white p-8 rounded-[2.5rem] shadow-xl border border-slate-100">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-xl font-bold">استخراج النصوص من الصور (OCR)</h2>
                <button onclick="backToMain()" class="text-slate-400">إغلاق ×</button>
            </div>
            <input type="file" id="ocrInput" accept="image/*" class="w-full p-4 border-2 border-dashed rounded-2xl mb-4">
            <div id="ocrStatus" class="text-sm text-indigo-600 font-bold mb-2"></div>
            <textarea id="ocrResult" class="w-full h-48 p-4 bg-slate-50 rounded-2xl border-none outline-none resize-none" placeholder="النص المستخرج سيظهر هنا..."></textarea>
            <button onclick="runOCR()" class="mt-4 bg-indigo-600 text-white px-8 py-3 rounded-xl font-bold w-full">بدء المسح الضوئي</button>
        </section>

        <section id="qr-sec" class="hidden-section bg-white p-8 rounded-[2.5rem] shadow-xl border border-slate-100">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-xl font-bold">مولد رموز QR</h2>
                <button onclick="backToMain()" class="text-slate-400">إغلاق ×</button>
            </div>
            <div class="flex flex-col md:flex-row gap-8">
                <div class="flex-1 space-y-4">
                    <input type="text" id="qrText" placeholder="أدخل الرابط أو النص هنا" class="w-full p-4 bg-slate-100 rounded-xl outline-none focus:ring-2 ring-indigo-500">
                    <button onclick="generateQR()" class="w-full bg-slate-900 text-white py-4 rounded-xl font-bold">توليد الرمز</button>
                </div>
                <div class="w-48 h-48 bg-slate-50 rounded-2xl flex items-center justify-center border" id="qrcode">
                    </div>
            </div>
        </section>

        <section id="pdf-sec" class="hidden-section bg-white p-8 rounded-[2.5rem] shadow-xl">
             <div class="flex justify-between items-center mb-6">
                <h2 class="text-xl font-bold">صانع ملفات PDF</h2>
                <button onclick="backToMain()" class="text-slate-400">إغلاق ×</button>
            </div>
            <textarea id="pdfContent" class="w-full h-64 p-6 bg-slate-50 rounded-2xl border-none outline-none mb-4" placeholder="اكتب محتوى الملف هنا..."></textarea>
            <button onclick="makePDF()" class="w-full bg-red-500 text-white py-4 rounded-xl font-bold shadow-lg shadow-red-100">تحميل PDF جاهز</button>
        </section>

        <section id="img-conv" class="hidden-section bg-white p-8 rounded-[2.5rem] shadow-xl">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-xl font-bold">تحويل صيغ الصور</h2>
                <button onclick="backToMain()" class="text-slate-400">إغلاق ×</button>
            </div>
            <input type="file" id="imgFile" class="hidden" onchange="previewImage(this)">
            <div onclick="document.getElementById('imgFile').click()" class="border-2 border-dashed p-10 rounded-3xl text-center cursor-pointer hover:bg-slate-50 transition-all">
                <p id="imgPlaceholder">اضغط هنا لرفع صورة</p>
                <img id="imgShow" class="hidden max-h-48 mx-auto rounded-lg">
            </div>
            <div class="grid grid-cols-3 gap-3 mt-6">
                <button onclick="convertImg('webp')" class="bg-indigo-50 text-indigo-600 py-3 rounded-xl font-bold hover:bg-indigo-600 hover:text-white">WEBP</button>
                <button onclick="convertImg('png')" class="bg-indigo-50 text-indigo-600 py-3 rounded-xl font-bold hover:bg-indigo-600 hover:text-white">PNG</button>
                <button onclick="convertImg('jpeg')" class="bg-indigo-50 text-indigo-600 py-3 rounded-xl font-bold hover:bg-indigo-600 hover:text-white">JPG</button>
            </div>
        </section>

    </div>

    <script>
        // --- Navigation ---
        function showSection(id) {
            document.querySelectorAll('section').forEach(s => s.classList.add('hidden-section'));
            document.getElementById('mainGrid').classList.add('hidden');
            document.getElementById(id).classList.remove('hidden-section');
        }

        function backToMain() {
            document.querySelectorAll('section').forEach(s => s.classList.add('hidden-section'));
            document.getElementById('mainGrid').classList.remove('hidden');
        }

        // --- OCR Logic ---
        async function runOCR() {
            const input = document.getElementById('ocrInput');
            const status = document.getElementById('ocrStatus');
            const result = document.getElementById('ocrResult');
            
            if (input.files.length === 0) return alert('اختر صورة أولاً');

            status.innerText = "جاري التعرف على النص... يرجى الانتظار";
            
            try {
                const { data: { text } } = await Tesseract.recognize(input.files[0], 'ara+eng');
                result.value = text;
                status.innerText = "تمت العملية بنجاح ✅";
            } catch (e) {
                status.innerText = "حدث خطأ أثناء المعالجة";
            }
        }

        // --- QR Logic ---
        function generateQR() {
            const text = document.getElementById('qrText').value;
            const container = document.getElementById('qrcode');
            container.innerHTML = "";
            if (!text) return alert("أدخل نصاً أولاً");
            
            new QRCode(container, {
                text: text,
                width: 180,
                height: 180
            });
        }

        // --- PDF Logic ---
        function makePDF() {
            const { jsPDF } = window.jspdf;
            const doc = new jsPDF();
            const content = document.getElementById('pdfContent').value;
            
            const lines = doc.splitTextToSize(content, 180);
            doc.text(lines, 10, 10);
            doc.save("smart-doc.pdf");
        }

        // --- Image Logic ---
        let currentImgData = null;
        function previewImage(input) {
            const file = input.files[0];
            const reader = new FileReader();
            reader.onload = function(e) {
                document.getElementById('imgShow').src = e.target.result;
                document.getElementById('imgShow').classList.remove('hidden');
                document.getElementById('imgPlaceholder').classList.add('hidden');
                currentImgData = e.target.result;
            }
            reader.readAsDataURL(file);
        }

        function convertImg(format) {
            if (!currentImgData) return alert("ارفع صورة أولاً");
            const canvas = document.createElement('canvas');
            const img = document.getElementById('imgShow');
            canvas.width = img.naturalWidth;
            canvas.height = img.naturalHeight;
            const ctx = canvas.getContext('2d');
            ctx.drawImage(img, 0, 0);
            
            const dataUrl = canvas.toDataURL(`image/${format}`);
            const link = document.createElement('a');
            link.href = dataUrl;
            link.download = `converted-img.${format}`;
            link.click();
        }

        // --- Clock ---
        setInterval(() => {
            const now = new Date();
            document.getElementById('clock').innerText = now.toLocaleTimeString('ar-SA');
        }, 1000);
    </script>
</body>
</html>
